/**
 * LiveCallPage
 * Route: / (default)
 * Provides the same functionality as ActiveCall but from the Dashboard flow.
 * When no sessionId is in URL, shows the dashboard start-call UI.
 */
export { default } from './Dashboard';
