# Pravaah OS — Root requirements (backend + pipeline)
# Install with: pip install -r requirements.txt

# Flask core
flask==3.0.3
flask-cors==4.0.1
flask-socketio==5.3.6

# WebSocket async support for SocketIO
simple-websocket==1.0.0

# Database
# (SQLite bundled with Python)

# Deepgram STT WebSocket
websockets==12.0

# HTTP clients (TTS + OpenRouter)
requests==2.32.3

# Environment variable loading
python-dotenv==1.0.1

# File validation
werkzeug==3.0.3

# PDF text extraction
pdfminer.six==20231228

# DOCX text extraction
python-docx==1.1.2

# Typing support
typing-extensions==4.12.2
